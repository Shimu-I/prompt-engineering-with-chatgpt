# 1.4

## 🧩 **Module 1.4 – Setting Up ChatGPT and the OpenAI API**

*(Section 1.2.1.4 of the syllabus)*

---

### **Part A: Concept Foundations**

### **1️⃣ The Purpose of Setup**

Before you can build, experiment, or deploy AI-powered workflows, you must understand how to **set up ChatGPT and the OpenAI API environment** properly.

This section bridges **conceptual learning** and **practical implementation**, helping you turn theory into hands-on engineering.

> 🧠 Analogy:
> 
> 
> Imagine learning to drive — studying traffic laws isn’t enough; you must know how to start the car, adjust the mirrors, and handle the steering.
> 
> Setting up ChatGPT and the API is exactly that — preparing the “vehicle” that lets you *drive generative AI* effectively.
> 

---

### **2️⃣ The Two Interfaces: ChatGPT vs OpenAI API**

Both **ChatGPT** and the **OpenAI API** give you access to the same family of powerful models (GPT-4, GPT-4-Turbo, etc.), but they serve different purposes.

| **Interface** | **Purpose** | **Best For** |
| --- | --- | --- |
| **ChatGPT App / Web** | Graphical interface for conversational use. | Education, prototyping, creative writing, analysis, idea exploration. |
| **OpenAI API** | Programmatic access via code. | Developers building applications, automation, or integrations. |

> 💡 Think of ChatGPT as the friendly front-end, and the API as the engine under the hood.
> 

---

### **3️⃣ Setting Up ChatGPT (Web or Desktop)**

**Step 1: Access the Platform**

- Go to [https://chat.openai.com](https://chat.openai.com/)
- Log in using Google, Microsoft, or email credentials.
- Choose your plan: **Free (GPT-3.5)** or **Plus (GPT-4)**.

**Step 2: Explore the Interface**

- **Model Selector:** Choose GPT-4 or GPT-3.5.
- **Conversation Sidebar:** Manage ongoing threads or save prompts.
- **Tools/Capabilities (GPT-4):**
    - Code Interpreter / **Advanced Data Analysis (ADA)**
    - **DALL·E** for image generation
    - **Browser** for live information
    - **File Upload** for document analysis

**Step 3: Optimize Usage**

- Enable “Memory” (if available) for persistent context across sessions.
- Organize conversations by module or project (e.g., “Prompt Engineering Course / Module 1”).
- Use **Custom Instructions** to personalize ChatGPT’s teaching or working style.

> 🎯 Goal: Treat ChatGPT like a personal AI lab assistant — capable of writing, analyzing, and experimenting interactively.
> 

---

### **4️⃣ The OpenAI API: Overview**

The **OpenAI API** allows developers and technical learners to **integrate LLMs into applications, scripts, or workflows** using code.

| **Key Concept** | **Definition** |
| --- | --- |
| **API** | Application Programming Interface — a gateway that allows programs to communicate with OpenAI’s models. |
| **API Key** | A unique identifier used for secure access to OpenAI services. |
| **Endpoint** | A specific URL that handles a particular request type (e.g., `/v1/chat/completions`). |
| **Payload** | The data (your prompt and parameters) sent to the API. |
| **Response** | The AI’s generated output in structured JSON format. |

> 🧩 In simple terms, the API is how your code talks to GPT — sending a request and receiving a response programmatically.
> 

---

### **5️⃣ Getting Started with the API**

### **Step 1: Create an OpenAI Account**

Visit [https://platform.openai.com](https://platform.openai.com/).

Once logged in, go to the **Dashboard → View API Keys** section.

### **Step 2: Generate an API Key**

Click **“Create new secret key.”**

Keep this key **private** — treat it like a password.

(If leaked, others could use your account credits.)

### **Step 3: Install the OpenAI Python Library**

In your terminal or notebook:

```bash
pip install openai

```

### **Step 4: Basic Python Setup Example**

Here’s a minimal code snippet to test your connection:

```python
from openai import OpenAI
client = OpenAI(api_key="your_api_key_here")

response = client.chat.completions.create(
    model="gpt-4-turbo",
    messages=[
        {"role": "system", "content": "You are a helpful AI assistant."},
        {"role": "user", "content": "Write a haiku about learning AI."}
    ]
)

print(response.choices[0].message.content)

```

✅ **Output Example:**

*“Neural thoughts unfold,
Patterns bloom in silicon —
Wisdom born of code.”*

> 🧩 Congratulations — you’ve just sent your first prompt via the API!
> 

---

### **6️⃣ Core API Parameters**

| **Parameter** | **Description** | **Example** |
| --- | --- | --- |
| **model** | Specifies which model to use (e.g., `gpt-4-turbo`). | `"model": "gpt-4-turbo"` |
| **messages** | Defines the conversation context (system, user, assistant). | `[{ "role": "user", "content": "Hello!" }]` |
| **temperature** | Controls creativity (0–1 range). | `0.7` for creative, `0.2` for factual. |
| **max_tokens** | Limits response length. | `max_tokens=200` |
| **top_p** | Adjusts randomness via nucleus sampling. | Keep default `1.0` |
| **stop** | Defines stopping points for generation. | `"stop": ["END"]` |

> 🔍 Each of these acts like a dial you can fine-tune — giving you direct control over style, length, and variability.
> 

---

### **7️⃣ Managing API Costs**

- **Pricing Model:** Charged per **1,000 tokens** processed (input + output).
- **Estimation Tip:** 1,000 tokens ≈ 750 words.
- **Best Practices:**
    - Summarize long prompts.
    - Use smaller models for basic tasks.
    - Cache or reuse intermediate outputs in large workflows.

---

### **8️⃣ Common Setup Pitfalls**

| **Issue** | **Likely Cause** | **Fix** |
| --- | --- | --- |
| “Invalid API key” | Key not set or expired | Recreate key in dashboard |
| “Rate limit reached” | Too many requests per minute | Use batching or increase rate limit |
| “Model not found” | Wrong model name or access restriction | Check documentation |
| “JSON error” | Improper payload formatting | Validate syntax before sending |

> 💡 Pro-Tip: Always wrap your API calls in a try/except block to catch these gracefully.
> 

---

### **Part B: Application and Examples**

### **Example 1 — Using ChatGPT for Prompt Design Practice**

Start with ChatGPT’s conversational interface to **draft and iterate prompts quickly** before coding them.

```
I’m designing a prompt for the OpenAI API that generates creative ad copy.
Can you help me structure it with variables for product name and tone?

```

✅ ChatGPT helps generate prompt templates and parameter-ready instructions.

---

### **Example 2 — Integrating API with Python**

Here’s a **dynamic prompt system** in code:

```python
def generate_ad(product_name, tone="friendly"):
    response = client.chat.completions.create(
        model="gpt-4-turbo",
        messages=[
            {"role": "system", "content": f"You are an expert ad copywriter with a {tone} tone."},
            {"role": "user", "content": f"Write a 3-sentence ad for {product_name}."}
        ]
    )
    return response.choices[0].message.content

print(generate_ad("AI-Powered To-Do App"))

```

✅ The function allows **prompt parameterization**, enabling dynamic generation.

---

### **Example 3 — Creating Structured Outputs**

You can ask the model to return data in **JSON** format for downstream use.

```python
response = client.chat.completions.create(
    model="gpt-4-turbo",
    messages=[
        {"role": "user", "content": "List 3 AI tools with name, category, and one-line description. Output as JSON."}
    ]
)
print(response.choices[0].message.content)

```

✅ Output Example:

```json
[
  {"name": "ChatGPT", "category": "Conversational AI", "description": "Generates text-based responses."},
  {"name": "DALL·E", "category": "Image Generation", "description": "Creates images from text prompts."},
  {"name": "Whisper", "category": "Speech-to-Text", "description": "Transcribes spoken language."}
]

```

> 🧱 This structure makes your outputs machine-readable and integration-ready.
> 

---

### **Part C: Reflection, Quiz, and Mini Project**

### **Reflection Prompt**

> Which mode best suits your current workflow — ChatGPT (interactive) or API (programmatic)?
> 
> 
> What advantages or limitations do you see in each for your goals?
> 

---

### **Quick Quiz**

| **Q#** | **Question** | **Type** |
| --- | --- | --- |
| 1 | What is the main difference between ChatGPT and the OpenAI API? | Short Answer |
| 2 | What is an API key, and why must it be private? | Short Answer |
| 3 | Which parameter controls the creativity of the model? | Short Answer |
| 4 | What Python library is used to access OpenAI models? | Short Answer |
| 5 | What are tokens used for in pricing? | MCQ |
| 6 | How would you prevent schema drift in API outputs? | Short Answer |
| 7 | Name two tools available in ChatGPT-4. | Short Answer |

---

### **Answer Key**

| **Q#** | **Correct Answer** | **Explanation** |
| --- | --- | --- |
| 1 | ChatGPT is a user interface; the API allows programmatic access. | Interfaces differ in purpose. |
| 2 | It authenticates access to OpenAI servers; sharing it risks unauthorized use. | Security measure. |
| 3 | Temperature | Higher = more creative, lower = more focused. |
| 4 | `openai` (OpenAI Python library) | Used for requests to models. |
| 5 | Token = billing unit for model input/output length. | Cost = per 1,000 tokens. |
| 6 | Add explicit instruction: “Output only valid JSON.” | Prevents formatting errors. |
| 7 | DALL·E, Advanced Data Analysis, Browser, File Upload. | ChatGPT-4 tools for multimodality. |

---

### **Mini Project: “Build Your First Prompt App”**

> Goal: Connect your local code to the OpenAI API and automate a real use case.
> 

| **Step** | **Action** | **Outcome** |
| --- | --- | --- |
| 1 | Set up an API key and Python environment. | API connection successful |
| 2 | Create a function to summarize text input. | Automated summarizer |
| 3 | Add a temperature parameter. | Dynamic tone control |
| 4 | Save responses to a file. | Persistent log of results |
| 5 | Test with 3 different texts. | Compare tone and accuracy |

✅ *Deliverable:* Screenshot or text file showing API responses for each test.

> 🧩 Instructor Extension: Evaluate code efficiency, error handling, and output consistency.
> 

---

### **Instructor Notes (Optional)**

| **Criterion** | **Score (1–10)** | **Focus Area** |
| --- | --- | --- |
| Setup accuracy |  | Correct API configuration |
| Security awareness |  | Handling of API key |
| Code clarity |  | Clean, documented code |
| Reflection insight |  | Practical understanding of differences between tools |

---

✅ **Summary Insight:**

> Setting up ChatGPT and the OpenAI API transforms you from an AI user into an AI builder.
> 
> 
> The interface (ChatGPT) helps you think interactively; the API lets you automate those thoughts at scale.
> 
> A skilled prompt engineer knows both worlds — how to **prototype ideas in ChatGPT** and **deploy them programmatically** for real-world impact.
> 

---

##